import FormWrapper from "../FormWrapper";
import { experience } from "../formFields";

export const AboutForm = () => (
	<FormWrapper formFields={experience} name="Experiences" />
);

export default AboutForm;
