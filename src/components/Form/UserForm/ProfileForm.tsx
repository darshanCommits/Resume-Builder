import FormWrapper from "../FormWrapper";
import { profile } from "../formFields";

export const ProfileForm = () => (
	<FormWrapper formFields={profile} name="Profile" />
);

export default ProfileForm;
